export type int = number;

/** A value between 0 and 1 for scaling numbers. */
// TODO uncapitalise
export type Scalar = number;

/** A key on the keyboard, such as `ARROWLEFT` or `CONTROL`. */
// TODO uncapitalise
export type Key = string;

/** A cardinal direction. */
export type Direction = "top" | "left" | "down" | "right";
